# Component Boundary

SCTL owns orchestration, validation, Class A/B commits, context export, canonical envelope rendering, return ingestion, and cycle progression.

This runtime delegate owns only mechanical runtime control: binding SCTL session IDs to WSL/tmux/Codex targets, delivering dispatch packets unchanged, capturing pane evidence, dropping return files under the contract return path, and terminating registered runtime sessions.
