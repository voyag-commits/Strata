Package checksums regenerated after the ADR_06_18 delegate control surface refactor.

`PACKAGE_CHECKSUMS.sha256` covers the packaged source, docs, tests, and compiled `dist/` files. Runtime artifacts, `.strata-runtime/`, and `node_modules/` are intentionally excluded.
