# P-0003 Ic Context Packet

Assignment: A-0001
Role: ic

## Authority

- Do not treat chat memory as governance truth.
- Use backend state and this packet as source of truth.
- Use `stratactl` for backend reads/writes when authorized.
- Do not perform ClassA, meaning-changing ClassB, policy/template, milestone, validation override, or destructive operations without Human Director approval.

## Sources

- No ClassB sources supplied.

## Required Output

Produce a report candidate with `report_version: "1.1"` if this session completes work.

