{
  "name": "strata-runtime-edge-launcher-delegate-component2-3",
  "version": "0.9.0",
  "description": "Strata Component 2 runtime-edge package using a local launcher delegate / BYOR provider instead of embedding Codex/DeepSeek bridge secrets.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "main": "dist/src/cli.js",
  "bin": {
    "strata-runtime-edge": "dist/src/cli.js",
    "strata-codex-delegate": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test --test-concurrency=1 --test-reporter spec dist/tests/*.test.js",
    "secret-scan": "node scripts/secret_scan.mjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "strata",
    "runtime-edge",
    "launcher-delegate",
    "byor",
    "codex-cli",
    "deepseek",
    "tmux",
    "adr0610"
  ]
}
